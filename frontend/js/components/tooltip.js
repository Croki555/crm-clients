// const contacts = document.querySelector('.contacts');

// contacts.addEventListener('mouseover', function(ev) {
//   if(ev.target.classList.contains('contacts__link')) {
//     let href = String(ev.target.getAttribute('href'));
//     if(href.includes('tel:') || href.includes('mailto:')) {
//       console.log(href.slice())
//     }
//   }
// })